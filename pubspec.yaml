name: google_sheets_update_example
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3

  gsheets: ^0.3.0
  http: ^0.13.3
  get: ^4.1.4
  flutter_staggered_grid_view: ^0.4.0
  flutter_html: ^2.1.0
  new_gradient_app_bar: 


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true